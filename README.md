# homebrew-spnego-proxy

Homebrew formula for [spnego-proxy](https://github.com/andrewesweet/spnego-proxy),
a SPNEGO/Kerberos authenticating HTTP proxy.

## Install

```sh
brew tap andrewesweet/spnego-proxy
brew install spnego-proxy
```

## Update

```sh
brew update
brew upgrade spnego-proxy
```
